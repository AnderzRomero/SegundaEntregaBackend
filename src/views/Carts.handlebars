<h1>carts list</h1>

{{#if carts}}
    <p> id carrito: {{Carts._id}}</p>
    <br>
    {{#each Carts.products}}
        <br>
        <p> id producto: {{this.id_product}}</p>
        <p> quantity producto: {{this.quantity}}</p>
    {{/each}}
{{else}}
    <p>No se encontró ningún carrito.</p>
{{/if}}